@model storefront.Models.ItemListViewModel
@{
    ViewBag.Title = "Items";
}

<h2>Items</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @for (var i = 0; i < Model.Items.Count; i++)
        {
            <tr>
                <td></td>
            </tr>
        }
    </tbody>


</table>